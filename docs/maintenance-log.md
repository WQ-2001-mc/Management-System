# NEXUS 维护日志

本日志记录不改变产品主版本的日常维护、性能优化与质量验证。

## 2026-08-04

### 体验优化

- 顶部全局搜索支持按页面名称和中英文关键词查找，并可通过 `Command/Ctrl + K` 快速聚焦和跳转。
- 明暗主题和侧栏收起状态在浏览器中持久保存，刷新页面后继续沿用用户偏好。
- 为导航收起、通知和主题按钮补充无障碍名称与操作提示，并移除头像对外部随机图片服务的依赖。

### 工程质量

- 新增页面搜索与界面偏好解析单元测试。
- 修正 README 中的 GitHub 克隆地址。

## 2026-07-31

### 优化

- 将 ECharts 从完整包引入改为按需注册，只打包当前页面使用的折线图、柱状图、饼图、漏斗图及必要组件。
- 分离图表实例初始化与配置更新：配置变化时复用现有实例，避免销毁后重复创建。
- 移除代码库未使用的 `@tanstack/react-query`、`dayjs` 和 `lucide-react` 直接依赖，精简安装内容和依赖维护面。

### 文档

- 在 README 中增加维护日志入口。

### 验证

- ESLint：通过
- TypeScript 类型检查：通过
- Vitest：6 个测试文件、14 项测试全部通过
- Next.js 生产构建：成功生成 15 条路由
- 当前构建环境中的未压缩客户端 JavaScript 总量由 4,244 KiB 降至 3,196 KiB（减少约 24.7%）；包含 ECharts 的单个路由产物由 1,148 KiB 降至 624 KiB（减少约 45.6%）
